#include<stdio.h>
#include<stdlib.h>

int main(){

//   int int1;

  // int i = scanf("%d", &int1);

  // printf("%d\n Trovati %d oggetti\n", int1, i);

   /*i = scanf("%d %c", &int1, &char1);

   printf("%d\n%c\n Trovati %d oggetti\n", int1, char1, i);*/
   char Buffer [100];
   char *string = NULL;
   while(fgets(Buffer, 100, stdin)){
      realloc(Buffer, strlen(
   }
   return 0;
}
